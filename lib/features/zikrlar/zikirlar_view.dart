import 'package:flutter/material.dart';

class ZikirlarView extends StatelessWidget {
  const ZikirlarView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ZikirlarView'),
      ),
      body: Center(
        child: const Text('ZikirlarView'),
      ),
    );
  }
}